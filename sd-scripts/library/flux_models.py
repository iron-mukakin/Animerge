# STUB: flux_models.py
# Flux専用モデル定義。Anima学習パスでは使用されない。
# flux_train_utils.py のトップレベルimportを通過させるためのスタブ。


class Flux:
    def __init__(self, *args, **kwargs):
        raise NotImplementedError("flux_models.Flux is not supported in Anima.")


class AutoEncoder:
    def __init__(self, *args, **kwargs):
        raise NotImplementedError("flux_models.AutoEncoder is not supported in Anima.")


class ControlNetFlux:
    def __init__(self, *args, **kwargs):
        raise NotImplementedError("flux_models.ControlNetFlux is not supported in Anima.")
